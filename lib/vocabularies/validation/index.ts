import {Vocabulary} from "../../types"

const defs: Vocabulary = [
  require("./const"),
  require("./limit"),
  require("./limitItems"),
  require("./limitProperties"),
  require("./limitLength"),
]

module.exports = defs
