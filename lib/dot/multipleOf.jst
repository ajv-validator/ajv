{{# def.definitions }}
{{# def.errors }}
{{# def.setupKeyword }}
{{# def.$data }}

var division{{=$lvl}};
if ({{# def.$dataNotType:'number' }}
    (division{{=$lvl}} = {{=$data}} / {{=$schemaValue}}) !== parseInt(division{{=$lvl}})) {
  {{# def.error:'multipleOf' }}
} {{? $breakOnError }} else { {{?}}
